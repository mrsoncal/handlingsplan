// Disabled LAN/P2P feature: this stub safely replaces the previous LiveSync component.
import React from 'react';

/**
 * LiveSyncCard (stub)
 * This component used to manage LAN/WebRTC syncing.
 * It is now intentionally disabled and renders nothing.
 */
export default function LiveSyncCard() {
  return null;
}
